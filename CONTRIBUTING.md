# 协作指南 - 图片上传流程

## 🤝 如何贡献图片

### 方法一：Fork + Pull Request (推荐)

1. **Fork 仓库**
   - 访问 https://github.com/xiaohuyyyy-tps/gmsins
   - 点击右上角 "Fork" 按钮
   - 创建你自己的副本

2. **克隆你的 Fork**
   ```bash
   git clone https://github.com/你的用户名/gmsins.git
   cd gmsins
   ```

3. **创建新分支**
   ```bash
   git checkout -b upload-2026-02-27
   ```

4. **添加图片**
   - 将图片放到 `webpage/pics/YYYY-MM-DD/` 目录
   - 例如：`webpage/pics/2026-02-27/your-photo.jpg`

5. **更新图库数据**
   ```bash
   cd webpage
   python scan.py
   ```

6. **提交更改**
   ```bash
   git add .
   git commit -m "Add photos for 2026-02-27"
   git push origin upload-2026-02-27
   ```

7. **创建 Pull Request**
   - 访问你的 Fork 页面
   - 点击 "New Pull Request"
   - 标题：`Add photos for 2026-02-27`
   - 描述：说明添加的图片内容

### 方法二：直接上传 (如果你添加为协作者)

如果你被添加为协作者，可以直接：

1. **克隆仓库**
   ```bash
   git clone https://github.com/xiaohuyyyy-tps/gmsins.git
   cd gmsins
   ```

2. **创建分支**
   ```bash
   git checkout -b upload-2026-02-27
   ```

3. **添加图片并提交**
   ```bash
   # 添加图片到 webpage/pics/YYYY-MM-DD/
   cd webpage
   python scan.py
   cd ..
   git add .
   git commit -m "Add photos for 2026-02-27"
   git push origin upload-2026-02-27
   ```

4. **创建 Pull Request**
   - 在 GitHub 上创建 PR，等待审核合并

## 📝 图片命名规范

- 使用有意义的文件名
- 避免特殊字符和空格
- 推荐格式：`描述.jpg` 或 `001.jpg`, `002.jpg`

## 🤖 自动化流程

### GitHub Actions 自动更新
当你提交包含图片的 PR 时，GitHub Actions 会：

1. **自动检测** 图片文件变化
2. **运行 scan.py** 更新图库数据
3. **提交更新** 自动更新 `gallery-data.json`
4. **PR 评论** 显示更新统计信息

### 触发条件
- **推送到 main 分支** - 直接更新
- **推送到 upload-* 分支** - 自动更新
- **Pull Request** - 预览更新并评论

### 无需手动操作
你只需要：
1. 添加图片到正确目录
2. 提交更改
3. 创建 Pull Request

其余步骤全部自动化！

## 📋 注意事项

- 确保图片内容合适
- 按日期正确分类
- 运行 `scan.py` 更新数据
- 创建清晰的 PR 描述
